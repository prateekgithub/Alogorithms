#!/usr/bin/env python3

def fixrange(MyList):

    for i in range(len(MyList)):
        tempList = MyList.copy()
        list2 = tempList.pop(i)
        for j in range(len(tempList)):
            if list2[0] <= tempList[0] <= list3[0]
